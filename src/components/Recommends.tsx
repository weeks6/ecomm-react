import { Book } from '../store/store'

type Props = {
    title: string,
    recommends: Book[] 
}

function Recommends({title, recommends}: Props) {
    return (
        <div className="">

        </div>
    )
}

export default Recommends
